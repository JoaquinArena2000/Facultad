#include <stdio.h>
#include <stdlib.h>
typedef struct
{
    int edad
    char nombnre
    int telefono
} registro
int main()
{ registro ALUMNO ;

    fflush (stdin);
    printf("Ingrese  wl nombre del alumno \n");
    gets (ALUMNO.nombre );
    printf ("ingrese la edad del alumno ");
    scanf("%d", &ALUMNO.edad);

    return 0;
}
